info:
  name: Add artist 2
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/artists
  body:
    type: json
    data: |-
      {
        "artistName" : "Abdelilah"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

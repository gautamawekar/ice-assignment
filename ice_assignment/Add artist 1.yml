info:
  name: Add artist 1
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/artists
  body:
    type: json
    data: |-
      {
        "artistName" : "gautam1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
